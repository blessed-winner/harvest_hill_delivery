services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: harvesthill
      POSTGRES_USER: harvesthill
      POSTGRES_PASSWORD: harvesthill_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
